language: generic
sudo: required
services:
  - docker

before_install: 
  - docker build -t gorillavila/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true gorillavila/docker-react npm run test